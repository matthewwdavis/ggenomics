% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_telplot.R
\name{geom_telplot}
\alias{geom_telplot}
\title{Telomere Plot Components for ggplot2}
\usage{
geom_telplot(
  chr_color = "#F8766D",
  chr_size = 6,
  tel_color = "black",
  tel_shape = 16,
  legend_title = "Telomere Size (bp)",
  text_size = 6,
  plot_title = NULL,
  x_axis_title = NULL,
  y_axis_title = "Chromosome Length"
)
}
\arguments{
\item{chr_color}{A character string specifying the color of the chromosome segments. Default is \code{"#F8766D"}.}

\item{chr_size}{A numeric value specifying the thickness of the chromosome segments. Default is \code{6}.}

\item{tel_color}{A character string specifying the color of the telomere points. Default is \code{"black"}.}

\item{tel_shape}{An integer specifying the shape of the telomere points. Default is \code{16}.}

\item{legend_title}{A character string specifying the title for the legend representing telomere size. Default is \code{"Telomere Size (bp)"}.}

\item{text_size}{A numeric value specifying the base size for plot text, such as axis labels and titles. Default is \code{6}.}

\item{plot_title}{A character string specifying the title of the plot. Default is \code{NULL} (no title).}

\item{x_axis_title}{A character string specifying the title of the x-axis. Default is \code{NULL} (no title).}

\item{y_axis_title}{A character string specifying the title of the y-axis. Default is \code{"Chromosome Length"}.}
}
\value{
A list of ggplot2 components for building a telomere plot in ideogram style.
}
\description{
This function creates a list of ggplot2 components for visualizing telomere size and chromosome length in a plot.
}
\details{
The function returns a list of ggplot2 layers and components:
\itemize{
\item Chromosome segments (\code{geom_segment}) are plotted from the telomere start positions to chromosome lengths.
\item Telomere points (\code{geom_point}) are added at the start and end positions of the telomeres, with sizes proportional to telomere lengths.
\item A customizable legend for telomere size.
\item Optional titles for the plot and axes.
}

Users can add this list of components to a ggplot2 object to generate a telomere plot.
}
\examples{
\dontrun{
library(ggplot2)

# Example dataset
df <- data.frame(
  Chromosome = factor(1:3),
  begin_telo_start = c(0, 0, 0),
  begin_telo_end = c(100, 200, 300),
  begin_telo_bp = c(100, 200, 300),
  end_telo_end = c(1500, 2000, 2500),
  end_telo_bp = c(500, 400, 300),
  Length = c(2000, 2500, 3000)
)

# Telomere plot
ggplot(df) +
  geom_telplot(
    chr_color = "blue",
    tel_color = "red",
    plot_title = "Telomere Visualization",
    x_axis_title = "Chromosomes",
    y_axis_title = "Chromosome Length (bp)"
  )
}

}
\seealso{
\code{\link[ggplot2:geom_segment]{ggplot2::geom_segment()}}, \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}, \code{\link[ggplot2:labs]{ggplot2::labs()}}, \code{\link[ggplot2:theme]{ggplot2::theme()}}
}

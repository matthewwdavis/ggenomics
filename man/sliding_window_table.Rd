% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sliding_window_table.R
\name{sliding_window_table}
\alias{sliding_window_table}
\title{Generate Sliding Windows From Table}
\usage{
sliding_window_table(mut_table, window_size = 10000, slide_size = 5000)
}
\arguments{
\item{mut_table}{A data frame containing mutation data. \strong{Must include columns named \code{CHROM} and \code{POS}.}}

\item{window_size}{A numeric value specifying the size of the sliding window (in base pairs). Default is \code{10000} (10 kb).}

\item{slide_size}{A numeric value specifying the step size for sliding the window (in base pairs). Default is \code{5000} (5 kb).}
}
\value{
A data frame with the original data and additional columns:
\describe{
\item{WINDOW_START}{Start position of the sliding window.}
\item{WINDOW_END}{End position of the sliding window.}
\item{POS_WINDOW}{Midpoint of the sliding window.}
}
}
\description{
This function calculates sliding windows for a mutation table, enabling downstream analysis of genomic regions.
It computes window start and end positions as well as the midpoint for each window, based on \code{CHROM} and \code{POS} columns.
}
\details{
\strong{Important:} The input data frame must contain columns named \code{CHROM} (chromosome) and \code{POS} (position).
}
\examples{
# Example input data
mut_table <- data.frame(
  CHROM = c("Chr1", "Chr1", "Chr1", "Chr2", "Chr2"),
  POS = c(100, 5000, 15000, 200, 8000)
)

# Generate sliding window table
sliding_window_table(mut_table, window_size = 10000, slide_size = 5000)

}
